pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "executando build"
            }
        }

        stage('Deploy em desenvolvimento') {
            when {
                branch 'develop'
            }
            steps {
               echo 'develop'
            }
        }
        stage('Atualiza em homolog') {
            when {
                branch 'homolog'
            }
            steps {
               echo 'homolog'
            }
        }
        stage("deploy em producao"){
            when {
                branch 'master'
        }
            steps {
                echo "producao"
            }
        }
    }
}
